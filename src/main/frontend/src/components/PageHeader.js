import React from "react"
import "../styles/PageHeader.css"

const PageHeader = () => {
    return(
        <div className={"PageHeader"}>
            <h2 className={"BankName"}>Neptune Banking</h2>
        </div>
    );
}

export default PageHeader